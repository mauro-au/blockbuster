<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, "Título", style: "display: block", class:"form-label mt-3", required: true %>
    <%= form.text_field :title, class:"form-control" %>
  </div>

  <div class="mt-3">
    <div class="d-flex justify-content-center">
      <%= link_to "<", movies_path, class: 'btn btn-outline-primary m-2' %>
      <% if action_name == 'edit' %>
        <%= link_to "Mostrar Película", @movie, class: 'btn btn-outline-primary m-2' %>
      <% end %>
      <%= form.submit (action_name == 'edit' ? 'Editar Película' : 'Crear Película'), class: "btn btn-primary m-2" %>
    </div>
  </div>
<% end %>
